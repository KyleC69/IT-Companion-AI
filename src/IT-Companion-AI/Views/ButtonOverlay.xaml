<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="ITCompanionAI.Views.ButtonOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ITCompanionAI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:ITCompanionAI.ViewModels">
    <UserControl.DataContext>
        <viewModels:ButtonViewModel />
    </UserControl.DataContext>

    <Grid>
            <Border BorderThickness="3" BorderBrush="CadetBlue" Padding="15" Margin="15" CornerRadius="30">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top"  Spacing="20">
      
                <Button Content="Harvest Api" Width="150" Height="40" Command="{Binding HarvestApiCommand}" IsEnabled="True"/>
                <Button Content="Harvest xml docx" Width="150" Height="40" Command="{Binding HarvestXmlDocxCommand}" IsEnabled="True"/>
                <Button Content="Get Doc Blocs" Width="150" Height="40" Command="{Binding GetDocBlocsCommand}" IsEnabled="True"/>
                <Button Content="Harvest Docs" Width="150" Height="40" Command="{Binding HarvestDocsCommand}" IsEnabled="True"/>

        </StackPanel>
            </Border>
    </Grid>
</UserControl>
